import 'package:flutter/material.dart';

class BossesDatabasePage extends StatelessWidget {
  const BossesDatabasePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Boss DataBase')),
      body: Center(child: Text('Details')),
    );
  }
}
