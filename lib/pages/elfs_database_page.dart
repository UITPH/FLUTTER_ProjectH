import 'package:flutter/material.dart';

class ElfsDatabasePage extends StatelessWidget {
  const ElfsDatabasePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Elf DataBase')),
      body: Center(child: Text('Details')),
    );
  }
}
